export default function DomainPage() {
  return (
    <div className="flex flex-1 items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-gray-900">Domain Selected</h1>
        <p className="mt-2 text-gray-500">Select a stream from the navigator to view content</p>
      </div>
    </div>
  );
}
